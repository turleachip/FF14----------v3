# FF14 レシピレベル推論君v3 プロジェクト起動手順

## フロントエンド起動（ターミナル1）

1. フロントエンドディレクトリに移動

```bash
cd frontend

```

2. 開発サーバー起動

```bash
npm run dev

```

## バックエンド起動（ターミナル2）

1. バックエンドディレクトリに移動

```bash
cd backend

```

2. 仮想環境の有効化

```bash
.\venv\Scripts\activate

```

3. FastAPIサーバー起動

```bash
uvicorn app.main:app --reload

```

## 確認方法

- フロントエンド: http://localhost:3000 にアクセス
- バックエンド: http://localhost:8000/docs にアクセス

## トラブルシューティング

- 仮想環境が見つからない場合は、以下のコマンドで作成：

```bash
python -m venv venv

```

- パッケージが足りない場合は、以下のコマンドでインストール：

```bash
pip install -r requirements.txt

```

このファイルをプロジェクトのルートディレクトリに配置することで、次回以降の起動時に参照できます。
